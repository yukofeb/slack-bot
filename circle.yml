machine:
  node:
    version: 6.9.2

deployment:
  procuction:
    branch: master
    codedeploy:
      slack-bot:
        application_root: /
        region: us-west-2
        deployment_group: prod
        deployment_config: CodeDeployDefault.AllAtOnce