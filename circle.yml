machine:
  node:
    version: 6.9.2

deployment:
  staging:
    branch: master
    codedeploy:
      slack-bot:
        application_root: /
        region: ap-northeast-1
        deployment_group: prod